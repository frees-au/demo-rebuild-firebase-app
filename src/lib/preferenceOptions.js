export const timezoneOptions = Object.freeze([
  { value: 'America/Sao_Paulo', label: 'Rio de Janeiro (UTC-3)' },
  { value: 'Australia/Melbourne', label: 'Melbourne (UTC+10/UTC+11)' },
]);

export const languageOptions = Object.freeze([
  { value: 'en', label: 'English' },
]);

export const defaultTimezone = timezoneOptions[0].value;
export const defaultLanguage = languageOptions[0].value;
